- name: "Generic: Set authorized key for {{ ansible_user }} copying it from current user"
  become: false
  authorized_key:
    user: "{{ ansible_user }}"
    state: present
    key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
    path: "{{ authorized_keys_path | default(omit) }}"
    manage_dir: yes
